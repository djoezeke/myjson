# ===================================================================
# Copyright © 2025 Sackey Ezekiel Etrue
# Distributed under the terms of the MIT license.
# ===================================================================

load("@rules_cc//cc:cc_library.bzl", "cc_library")
load("@rules_license//rules:license.bzl", "license")

#===========================
# Build C++ Library        #
#===========================

cc_library(
	name = "myjson",
	srcs = [
		"src/myjson.cpp",
	],
	hdrs = glob(["include/myjson/*.hpp"]),
	visibility = ["//visibility:public"],
	includes = ["include"],
)

#===========================
# Build C Library          #
#===========================

cc_library(
	name = "myjsonc",
	srcs = [
		"src/myjsonc.cpp",
	],
	hdrs = glob(["include/myjson/*.h", "include/myjson/*.hpp"]),
	visibility = ["//visibility:public"],
    includes = ["src/"],
    linkstatic = True,
	includes = ["include"],
    deps = [":myjson"],
)

# Packaging – create a zip with headers and libraries
genrule(
	name = "dist_zip",
	srcs = ["README.md", "LICENSE"],
	outs = ["myjson-${PROJECT_VERSION}.zip"],
	cmd = "zip -r $@ include src README.md LICENSE || true",
)

# ===================================================================
